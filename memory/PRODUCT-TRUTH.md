# PRODUCT TRUTH — DIMA Now

Evidence class: local source, automated test output, emulator observation, and explicitly identified physical-device observation are kept distinct.

## Implemented

### Android project bootstrap — verified — 2026-08-26

Evidence: `app/build.gradle.kts` declares compile/target 36 and min 31; generated baseline `gradlew.bat test` completed successfully on 2026-08-26.

Checked: 2026-08-26.

### Shared guidance and editable local state — verified — 2026-08-26

Evidence: JVM tests cover the 60-minute boundary, start/during/end transitions, Tuesday adjacency, no-class dates, final-class return, and reachable two-leg transfers. API 36 instrumented tests cover Room seeding/editing and exact Compose guidance strings. The emulator rendered the seeded dashboard without a process crash.

### Location, permissions, and recovery — source, emulator, and V2 Galaxy install verified — 2026-08-27

Evidence: `LocationResolver` JVM tests cover the approved YEIN/MAIN/ONE_ROOM polygons, school-over-ONE_ROOM priority, nearest-center overlap, test-mode GPS override, last-zone retention, and explicit all-geofence exit. API 36 Room tests prove `CAMPUS_ZONES_V2_USER_2026_08_27` installs 5/6/9-vertex polygons while preserving courses and guidance pause. The approved GeoJSON SHA-256 is `90A0D9E51129D2481B2945CC78800B9E34D24D42AE7B33536A0A06512D8EA094`; wake-up circles enclose the farthest vertex with approximately 107–110 m margin. After `adb install -r`, a read-only Galaxy Room check found the exact V2 centers, 5/6/9 vertices, 550/570/680 m wake radii, and retained user data (5 current courses, 605 shuttle rows, 5 meal days). Test mode visibly ignored GPS and selected MAIN, then disabling it restored the GPS-derived YEIN zone. Physical-site dwell/exit/overlap transitions remain unobserved.

### Official shuttle source and widget — live-source verified — 2026-08-26

Evidence: the physical Galaxy performed one official-source refresh, visibly entered a serialized loading state, and retained 605 weekly-expanded rows/575 direction-aware service-day/origin/destination/time slots with a new KST success time. The older 505 count ignored destination and is not used by the current projection. Parser fixtures cover A/B/C table shapes. Guidance tests cover direction filtering, duplicate user slots, and reachable transfers. The classic RemoteViews widget renders only whole remaining minutes, schedules a cached minute-boundary recomputation, promotes passed departures, and has no countdown Chronometer.

### Live guidance surface — source, emulator, and Galaxy rendering verified — 2026-08-27

Evidence: the Galaxy reported manifest permission, promotion request, promotable characteristics, LOW (not MIN) channel, and `canPostPromotedNotifications=true`. The unavailable Samsung promoted-settings Activity safely fell back to `AppNotificationSettingsActivity` without an exception. A gated physical-device test notification was visibly rendered as a status-bar live countdown pill, an AOD card, and a lock-screen Now Bar item on the SM-S918N when Developer options > all-app live information was enabled. With only that setting disabled, the same ongoing notification remained posted but DIMA Now was absent from the lock-screen Now Bar and the promoted-notification app-op recorded a rejection; Developer options and USB debugging remained enabled throughout and the setting was restored after the comparison. DIMA Now remained absent from Samsung's normal Live notifications app list, so ordinary-list enrollment is Samsung-controlled. API 36 tests now verify selectable countdown/classroom compact content, selectable course/classroom first-line order, and exact `시작까지`/`종료까지` expanded wording. The physical permission screen exposed both option groups, but their final Samsung rendering during production guidance is still pending.

### Official cafeteria source, on-device OCR, validation, and widget — live-source and Galaxy verified — 2026-08-27

Evidence: the official DIMA homepage exposes only five recent Instagram posts, so the 2026-08-24 cafeteria post had rolled out of that discovery window by 2026-08-27. A red-green parser fixture now covers the official profile's login-free public feed metadata fallback while retaining the public post embed as the carousel/image source. On the SM-S918N, one serialized refresh visibly entered `새로고침 중`, found shortcode `DcaCs3yE6So`, ran Korean ML Kit OCR locally, validated 2026-08-24 through 2026-08-28, and stored five meal days. The UI showed the five menus, a KST last-success instant, and `캐시: 8/24~8/30`; these persisted across process restart. The displayed five dates, hours, and every menu line were visually compared with the official second carousel slide and matched. Instrumented cache-protection testing confirms an invalid candidate does not replace the last valid week.

### UX and cache improvements — test and Galaxy verified — 2026-08-26

Evidence: the physical app showed Korean weekday grouping, human-readable KST freshness, weekly meal range semantics, truthful shuttle cache labels, and deduplicated first/last annotations. Compose testing covers loading/debounce and exact display strings. Automatic guidance ignores a previously stored false value and no user toggle remains. Adaptive and monochrome app icons and compact meal widget resources passed Android resource compilation/lint; launcher add/resize remains manual.

### Timetable time picker and Live presentation preferences — test and Galaxy verified — 2026-08-27

Evidence: red-green JVM/API 36 tests verify persisted Live presentation choices and exact notification title/detail/compact payloads. Compose tests verify both settings groups and the absence of free-form `HH:mm` fields. On the physical Galaxy, `수업 추가` displayed `시작 10:00` and `종료 11:00`; tapping start opened Android's `시작 시간 선택` 24-hour picker with Cancel/Confirm controls. The dialog was dismissed without saving, and the process/activity remained healthy.

### Complete shuttle week and current meal week — test and Galaxy verified — 2026-08-27

Evidence: Compose tests feed literal `ShuttleSource` and `MealSource` data through the public data screen. They verify all service days/origins are present without filter controls, user-visible route-time duplicates collapse, first/last annotations remain, only the current Monday-through-Sunday meal week is shown, and missing dates are truthful. On the physical Galaxy, the retained official cache rendered 605 weekly-expanded rows/575 direction-aware user slots as weekday route groups; Monday included `엔터관 → 본관` and `본관 → 엔터관` with absolute times and first/last annotations. The same screen showed the validated 2026-08-24–2026-08-30 meal week and weekend no-menu cards. Captured UI contained no forbidden stadium-side label.

### Lock-invariant classroom Live Update — Galaxy transition verified — 2026-08-27

Evidence: the pre-fix physical loop showed `shortCriticalText=덕성관 510-1` before screen off and `shortCriticalText=시작까지 53분 · 덕성관 510-1` after screen ON while the keyguard was showing. Android documents this field as exclusively for the compact chip, so independent red-green `LiveSurfaceController` cases now require `덕성관 402` for both course-first and classroom-first orders even when `deviceLocked=true`. After `adb install -r`, five consecutive SM-S918N screen off/on cycles all retained `shortCriticalText=덕성관 510-1`. The visible AOD/lock-screen card showed `13:00 · 프리젠테이션영어` and `덕성관 510-1`; the notification body separately retained `시작까지 42분 · 덕성관 510-1`. One UI's collapsed Now Bar reuse of chip text means it can omit the countdown, but it no longer corrupts the configured classroom pill.

### Automated acceptance — verified — 2026-08-27

The stale `DataSourceScreenTest` mismatch has been replaced with current Shuttle/Meal public-screen tests. The latest full evidence is recorded in “Shared minute guidance follow-up” below.

## Pending live-device acceptance

- Remaining production presentation checks: normal unlocked classroom-pill fit/truncation and the countdown-pill option. Classroom persistence across AOD/keyguard screen cycles is verified; One UI's collapsed card intentionally receives only the classroom chip value.
- Physical geofence transitions at 예인관, 본관, and 원룸촌, including dwell and arrival cancellation.
- Two-countdown accuracy within one minute across real trips and final-class return behavior.
- One UI Home add/resize verification for both widgets.
- One-day battery observation. The official meal slide comparison is complete; only the One UI widget layout remains pending under the widget item above.

### Shared minute guidance follow-up — automated and Galaxy smoke verified — 2026-08-27

Evidence: final regression has 68 passing JVM tests. API 36 emulator instrumentation reports 41 cases with 39 executed passes and two explicitly gated Samsung visual-notification probes skipped; Android-test compilation, `lintDebug`, and `assembleDebug` pass. The public tests cover common ceil-minute calculation, route-variant deduplication without losing simultaneous destinations, home-base return selection, the class +30-minute transition, inclusive and until-disabled pause ranges, fixed polygon priority, test-mode GPS override, widget minute-boundary planning, current five-tab UI, source-screen cleanup, policy refresh, both Now Bar settings actions, and non-duplicated shuttle-only notification payloads.

The final APK SHA-256 is `358917BC66A9AEE77F3DC1A2C1D1A854093BC2828E24125236F9CD13AE171F38`. It was installed with `adb install -r` on the physical SM-S918N/API 36, retained the user's current courses and source caches, and cold-launched without an immediate FATAL, ANR, or Room migration failure. The Now Bar guide's two setting actions were then exercised on the Galaxy: both retained PID 14037 and opened the corresponding Samsung Settings Activity without a FATAL exception. A separately approved synthetic promotion probe passed: Samsung set `FLAG_PROMOTED_ONGOING`, the unlocked screen visibly showed the blue countdown pill, and the lock screen visibly showed a DIMA Now bar. The expanded synthetic item showed only the app name, so production class-detail and AOD layout remain pending. The synthetic notification and test-only package were removed afterward.

### First/last, bottom actions, pause choices, and background-start safety — test and Galaxy verified — 2026-08-27

Evidence: independent red-green Compose tests require `08:10 (첫차)` and `09:10 (막차)`, verify each data-management card is below both content and source status, and expose only the four approved pause choices. JVM and Room tests prove an until-disabled pause suppresses later guidance and round-trips without a schema migration. On the physical SM-S918N, the Thursday timetable visibly rendered `17:45 (막차)` in red, source status before `셔틀 데이터 관리`, the five weekday menus before `식단 데이터 관리`, and the exact four-choice pause dialog. The current UI reports 605 raw weekly-expanded rows and 575 direction-aware service-day/origin/destination/time slots.

During the first full physical run, the crash buffer exposed `ForegroundServiceStartNotAllowedException` when a package-replacement refresh tried to start `LiveMinuteUpdateService` from the background. A public `LiveSurfaceController` red-green case now requires safe degradation. After the fix, 55 JVM tests, `lintDebug`, both debug APK assemblies, and all 34 API 36 instrumented cases passed; the two Samsung visual probes remained assumption-gated in the full runner. A clean replace install and cold launch left PID 30897 alive, retained the Room database and DataStore files, and produced an empty crash buffer. APK SHA-256 is `FEAAD39C4B40467075609BF8983FBC48B5C86E590A21BFF3690F5089B6485A21`.

### Fixed polygons, test mode, source-screen cleanup, and automatic refresh policy — emulator and Galaxy verified — 2026-08-27

Evidence: the user-supplied final GeoJSON is preserved under `artifacts/zone-boundary-capture`. Three independent `LocationResolver` red-green slices verify the expanded approved polygon interiors, and API 36 Room tests verify atomic zone-row replacement without losing timetable or pause data. Compose tests verify the absence of dashboard/source footers, the top-right debounced refresh controls, KST source details in Settings, meal operating-state copy, course-delete confirmation, and the two-line Now Bar setup guide. JVM policy tests verify current-week shuttle suppression, previous-week refresh, meal-expiry refresh, and current-week meal suppression. Final verification is 68 passing JVM tests plus 39 API 36 emulator cases with 37 passes and the two Samsung-only cases assumption-skipped, with Android-test compilation, lint, and APK assembly green. The final APK SHA-256 is `BE83D5305C6FDFFB794036FE0D2EF9B8A8693E049E76898EA029187D2301164B`. The V2 build was replace-installed on the authorized SM-S918N, retained app data, passed UI/test-mode/deletion-guide smoke checks, and produced no immediate FATAL or ANR. A separately gated physical promotion test received `FLAG_PROMOTED_ONGOING` and visibly rendered the unlocked pill and lock-screen DIMA Now bar; production class detail and AOD layout remain pending.

### Balanced runtime optimization and optimized install - automated, emulator, and Galaxy smoke verified - 2026-08-28

Evidence: 88 JVM tests pass; `compileDebugAndroidTestKotlin`, `lintDebug`, `assembleDebug`, and `assembleOptimized` pass; API 36 instrumentation reports 47 cases with 44 passes and three intentional Samsung-physical/live-network skips. Unit and integration tests cover system-time ticker resynchronization, coalesced serialized runtime refresh, stale guidance-alarm cancellation, indexed-versus-List shuttle equivalence, one shared widget minute plan, visible-route ticker selection, and KST meal semantics. Compose compiler metrics for the optimized variant report strong skipping enabled, 159 skippable composables, and only the expected minute/animation state helpers as non-skippable.

The generated Baseline/Startup Profile journey covers cold launch, all five tabs, and shuttle scrolling without network/OCR. On the same API 36 emulator, five-iteration Macrobenchmark medians changed from 13,877.2 ms compilation-none cold start to 7,617.3 ms with Baseline Profile (45.1% lower); frame CPU P50 changed 266.5 to 193.4 ms and frame overrun P50 472.0 to 302.1 ms. These are emulator comparisons, not physical latency claims. The final optimized APK is 48,659,897 bytes versus debug 114,656,465 bytes (57.6% smaller), contains `assets/dexopt/baseline.prof`/`.profm`, and has SHA-256 `E2515F6BAE40BC49C51034BB45CD6ED03BA6DE10613A4FE09E82015A652BBA25`.

An exact-serial `adb install -r` on SM-S918N/API 36 succeeded using the same signing certificate. Cold launch reported 324 ms and PID 6826 remained alive. Read-only UI hierarchy checks opened all five tabs and proved retained course, 605-row shuttle projection, validated current meal, notices, and effective YEIN location. `dumpsys alarm` showed one active `UPDATE_ALL_WIDGETS_MINUTE` alarm plus the separate next guidance boundary; no legacy provider minute alarm was active. No immediate DIMA Now FATAL/ANR was found. This smoke did not re-certify OEM-controlled Now Bar/AOD rendering, physical geofence transitions, or one-day battery behavior.

### Deterministic Back navigation - emulator and Galaxy verified - 2026-08-28

Evidence: API 36 tests navigate Shuttle -> Meal -> Settings and require one Back to select Dashboard, then one Back to destroy `MainActivity`; another case requires Back to dismiss the course editor while Timetable remains selected, followed by Back to Dashboard. The full API 36 suite has 46 passes and three intentional Samsung/network skips. After `adb install -r` on SM-S918N, ADB/UI-hierarchy observation proved Settings -> Back selected Home and a second Back resumed One UI Home; Timetable -> course editor -> Back dismissed only the dialog, then Back selected Home. No DIMA Now FATAL/ANR appeared. The current optimized APK SHA-256 is `693CD96CE71BCDEE341254CC530691B9FECA11533D7915B96FF97A4D20F8C816`.

### Private GitHub source and v1.0 direct-install release - published and verified - 2026-08-28

Evidence: the authenticated GitHub account `winter1l` owns `winter1l/DimaNow`; the GitHub connector returned `visibility=private`, full admin/push permission, default branch `main`, and the published `README.md` content. Local caches, toolchains, SDK paths, device-derived databases/UI dumps, and APK build folders are ignored and were not included in source history. A fresh `testDebugUnitTest lintDebug assembleOptimized` run passed with 88/88 JVM tests. GitHub Release `v1.0` is published (not draft or prerelease) with `DIMA-Now-v1.0-optimized-20260828.apk`, 48,676,281 bytes, and GitHub's asset digest `sha256:78d39e715dcd42d6c322d5938dc07d78b399423fa94536eb00c7a8b91474a9ca`. `apksigner` verified APK Signature Scheme v2; the signer remains the Android debug certificate, so this is a personal direct-install artifact rather than Play Store production signing.

### Public static data and Gemini meal OCR - hosted Actions and Pages verified - 2026-08-30

Evidence: commit `71a9478` replaced the GitHub-hosted Tesseract meal step with one schema-constrained `gemini-3.5-flash-lite` image request while retaining the published `MealPayload` v1 contract and last-good cache protection. The local `.env` is ignored, `.env.example` contains only the variable name, and the repository Actions secret list confirms `GEMINI_API_KEY` exists. Local verification passed 79 app JVM tests, 17 pipeline tests, Android-test compilation, `lintDebug`, `assembleDebug`, and `assembleOptimized`. GitHub validation run `33316317176` and the manually dispatched meal publish run `33316410514` completed successfully; its log contained no Google API-key-shaped value. The deployed Pages manifest reports meal revision 6 and `READY`, published at `2026-08-30T14:17:03.363106625Z`. Its content-addressed payload SHA-256 `6c8584781f16ce42562d27c319e6c8c6e51982e4fcc75ef9bcfd5ea15e5ffb1c` matches the downloaded bytes and contains five weekday menus for `2026-08-24` through `2026-08-28`, with week range `2026-08-24` through `2026-08-30`.

### Outgoing-only tab fade - API 36 emulator verified - 2026-08-31 (SUPERSEDED by D-042: both pages fade again; this entry is historical evidence only)

Evidence: commit `23b0296` restores a 180 ms fade only to the outgoing tab while keeping the incoming tab fully opaque and above it. Card-level staggered entrance remains translation-only, so its elevation surface is not faded. On the API 36 `medium_phone` emulator, the focused `CardShadowTransitionTest` and `AppBackNavigationTest` run completed 3/3 cases with zero failures.

### User-submitted dormitory meal pipeline - automated and GitHub configuration verified - 2026-08-31

Evidence: the Meal screen separates `본관 학생식당` and `기숙사`; a missing target week exposes only the concise `사진 올리기` path with system Photo Picker and camera capture. Submission preflight refreshes the static manifest, serializes duplicate checks, caps JPEG/PNG/WebP uploads at 15 MiB, and obtains an expiring GitHub user token through Device Flow stored with Android Keystore encryption. GitHub App `DIMA Now Meal Upload` is installed only on `winter1l/DimaNow`; the verified permissions page shows `Contents: Read and write` plus GitHub-mandatory `Metadata: Read-only`, Device Flow enabled, no webhook, and no other selected permission. The public Client ID is embedded; no client secret, private key, user token, or Gemini key is stored in the app or repository.

The Actions pipeline makes two schema-constrained `gemini-3.5-flash-lite` calls: HIGH-thinking dormitory/full-table validation with the exact approved Korean keys, followed by separate structured OCR. Duplicate and stale-week candidates publish a status without replacing last-good Pages data. App unit tests report 101/101 passing; pipeline tests report 31 passing plus one explicitly gated live probe; final Android-test compilation, `lintDebug`, debug/optimized assemblies, and the API 36 suite pass. The final emulator suite reports 73 cases, 70 passes, and three intentional Samsung-physical/live-network skips.

Live GitHub Actions run `33343440547` processed the supplied 2026-08-24 through 2026-08-28 photo with the repository Gemini secret and completed the verification/OCR and Pages deployment steps. The public status file reports `REJECTED` with `이번 주 기숙사 식단표가 아니에요`; the Pages manifest still has no `dorm_meal` descriptor, proving the stale candidate did not become shared current-week data. GitHub Release `v1.3` publishes `DIMA-Now-v1.3-optimized.apk`, 4,793,181 bytes, with asset digest `sha256:06ebdc8e668a5a3304a05d79efe11a7524d44488f85b169da0c297e50518b9b3`, matching the local artifact. The APK is package `com.example.dimanow`, version 1.3 (4), signed by the existing Android debug certificate with APK Signature Scheme v2. An exact-serial fresh install on SM-S918N/API 36 succeeded; PID 2117 remained alive with `MainActivity` resumed and the crash buffer and `lastanr` were empty. Because no prior user-0 package existed at install time, this observation is a new install rather than replace-install data-retention evidence. After the user selected the home base, read-only UI hierarchy checks opened Home, Timetable, Shuttle, Meal, and Settings. Meal visibly separated `본관 학생식당` and `기숙사`; the missing current dormitory week showed `사진 올리기`, preflight retained the empty state, and the concise `사진 선택` / `카메라 촬영` chooser opened the Android Photo Picker and Samsung Camera respectively. No image was submitted during this UI smoke, PID 2117 remained alive, and the final crash buffer contained no DIMA Now FATAL or ANR.

## Permanently excluded

- Everytime scraping/login automation, a separately operated backend/server, and store release. Dormitory menus are accepted only from explicit user photo submissions through the verified GitHub/Gemini pipeline.

### Anonymous dormitory submission, weekly cafeteria schedule, and v1.4 candidate — automated/emulator/hosted verification — 2026-08-31

Evidence: v1.4 removes the Android GitHub Device Flow and user-token store. Its only client endpoint is the deployed Cloudflare Worker `dima-now-meal-upload.dima-now-chc01.workers.dev/v1/dormitory-meals`; the Worker retains its GitHub App private key and rate-limit salt only as Cloudflare Secrets, while the downloaded private-key file was removed after secret injection. A live unsupported-content POST returned HTTP 415. One explicitly authorized JPEG submission returned HTTP 202 with submission ID `2f4363e1-d2c9-4221-b44a-04a2c0ddec8c`; the `dorm-submissions` GitHub Actions run `33370821621` completed successfully, and its public Pages status is `REJECTED` with `이번 주 기숙사 식단표가 아니에요`. That photo represented 2026-08-24 through 2026-08-28, so the expected rejection confirms a stale week is not published or used to replace valid data.

Local v1.4 verification passed `testDebugUnitTest`, `:data-pipeline:test`, `compileDebugAndroidTestKotlin`, `lintDebug`, `assembleDebug`, `assembleOptimized`, and the Worker contract tests (3/3). API 36 `medium_phone` instrumentation completed 73 tests, 0 failures, with three intentional Samsung-physical/live-network skips. The optimized APK is version 1.4 (5), 4,776,821 bytes, SHA-256 `E683D36203BA77037A7F0431D38157A791BAC3C7BBED15936966D16B40F7E5DA`. A replace install on `emulator-5554` cold-launched `MainActivity`; PID 12589 stayed alive and the captured crash buffer had no DIMA Now FATAL or ANR. No Galaxy was attached during this final v1.4 check, so physical replace-install and Samsung Now Bar evidence remain pending.

Source commit `8452383` is published to `main`. GitHub Release [`v1.4`](https://github.com/winter1l/DimaNow/releases/tag/v1.4) is public, non-draft, and non-prerelease; its single optimized asset is 4,776,821 bytes with GitHub digest `sha256:e683d36203ba77037a7f0431d38157a791bac3c7bbed15936966d16b40f7e5da`, matching the local hash. The pushed Actions validation run `33372618812` and data-publish run `33372618799` both completed successfully.

### v1.4 Galaxy replace-install smoke — physical device verified — 2026-08-31

Evidence: exactly one authorized physical ADB target, `R3CW203NFSL` (`SM-S918N`), was present. `adb -s R3CW203NFSL install -r dist/DIMA-Now-v1.4-optimized.apk` succeeded, retaining the existing package data path. `dumpsys package` reports version 1.4 (5); `MainActivity` resumed warm in 178 ms, PID 26578 remained alive, and the post-launch crash buffer/FATAL/ANR search was empty. A physical screenshot shows the rendered Home tab with effective `엔터관`, the current shuttle card, the `기숙사` empty-meal state, notices, and the five-tab navigation. This is an installation and UI smoke only; it does not certify Samsung Now Bar/AOD rendering, live anonymous upload from the phone, real geofence behavior, or battery life.

### Foreground LMS classes and encrypted automatic login — automated and API 36 emulator verified — 2026-08-31

Evidence: the primary navigation now exposes `홈`, `시간표`, `수업`, `셔틀`, and `식단`, while Settings opens from the shared top-right gear and returns to its originating tab. The optional `수업` screen stores opted-in credentials only as Android Keystore AES-256-GCM ciphertext, keeps LMS cookies out of Room/DataStore/logs, limits automatic login to one serialized foreground attempt, and uses a separate last-good `lms-cache.db`. Notices, assignments, materials, sanitized details, and same-host attachments are read-only; attachment saving uses the system document picker.

The complete API 36 instrumentation run reported 73 tests, zero failures, and three intentional Samsung-physical/live-network skips. After the final session-cache refinement, the focused credential, Room cache, and Classes UI tests passed 3/3. JVM tests, Android-test compilation, `lintDebug`, and `assembleOptimized` passed. The final optimized APK is 5,121,665 bytes with SHA-256 `A1F5007E4ABDA359FE5E4F7A26A77B44C12802F24D19772F3097FF9F14D185FB`. A replace install on the API 36 `Medium_Phone` emulator retained package data, PID 8078 remained alive, and the crash buffer contained no immediate DIMA Now FATAL or ANR. UI hierarchy evidence confirms the `수업` login form, `학번`, `비밀번호`, `자동 로그인`, and exact `이 기기에 암호화해 저장합니다` disclosure. A real authenticated LMS session, current private course-route compatibility, and an attachment download still require the user to enter credentials and are not claimed by this emulator-only evidence.

### Complete current-term LMS history and local read state — automated/emulator verified; live catalog pending unlock — 2026-09-01

Evidence: parser tests accept the verified rendered-course JSON shape, JavaScript notice/material rows, and forward pagination forms. Room source tests prove a rendered catalog survives raw-dashboard omission, every page is merged, an empty or To-Do-only dashboard requests one rendered pass, a failed board kind preserves its previous rows, and opening a detail marks the stable item key read across refresh. The Classes UI exposes `모두`, `안읽음`, and `읽음` and a keyed lazy-list test scrolls through 80 fixture items.

The final app JVM suite is 120/120; the pipeline suite is 32 passes plus one gated live probe; Android-test compilation, lint, debug, and optimized builds pass. API 36 instrumentation reports 79 total cases with 76 passes and three intentional Samsung/live-network skips. Optimized APK `app/build/outputs/apk/optimized/app-optimized.apk` is 5,138,489 bytes with SHA-256 `935EF62BF694B0AEB87A63FD8814D497CCD7863CECC00F62D05896AB7BA756BC`.

The exact Galaxy `R3CW203NFSL` accepted `adb install -r`, retained package data, reports v1.4 (5), cold-launched in 496 ms, kept PID 29773 alive, and emitted no immediate app FATAL/ANR. The device was keyguard-locked during final verification. A read-only pre-final debug cache audit showed schema v3 but still zero courses and one unread To-Do assignment; therefore a successful foreground portal catalog extraction, nonzero all-history counts, and attachment download are explicitly not yet claimed.

### LMS `전체 학습` rows and app-owned detail — automated/emulator verified; live Galaxy sync pending — 2026-09-01

Evidence: parser fixtures reproduce the verified LMS shape of one `.learn_element_detail_list` container with multiple `<li>` rows and prove every row is retained in server order. Independent fixtures cover all nine LMS type codes, same-host route generation, list-to-detail resolution, matching-row fallback, executable-markup removal, JavaScript board attachments, and direct official download links. Room source tests prove the exact `?to_do_type=all` endpoint is the only list source, no per-course board enumeration occurs, stored rendered courses map raw rows, local read state survives refresh, and a list-backed assignment opens its matching article and attachment inside the source.

### LMS today agenda, completion state, and detail revalidation — automated/API 36 emulator verified; live account and Galaxy pending — 2026-09-01

Evidence: JVM fixtures prove server-rendered completion/incompletion links and rendered POST fields are used without guessed values, KST today-plus-three grouping boundaries, attachment-size parsing, and existing parser/security behavior. API 36 Room tests prove successful status merging, partial optional-status failure preservation, initial-baseline/no-false-NEW behavior, NEW/UPDATED persistence until open, atomic read/change clearing, v3-to-v4 preservation with `UNKNOWN`/`NONE` defaults, fresh detail revalidation, attachment-change detection, and offline cached-detail fallback. Compose tests prove Today is default, All retains prior filters/history, authoritative badges render, and completed items start collapsed.

Verification passed 130 app JVM tests, 33 pipeline tests with one intended gated skip, Android-test compilation, lint, debug and optimized assembly. The API 36 emulator finished 89 instrumentation cases with 86 passes and the existing three intentional Samsung/live-network skips. Optimized v1.5 (6), 5,171,257 bytes, SHA-256 `565806F95ECE294072AE7F5D5F101BBAE7659A93924A6E6B6B00740D5FDAA94A`, was replace-installed on `emulator-5554`; `MainActivity` remained resumed with PID 18648 and no captured DIMA Now FATAL/ANR. No physical Galaxy was connected, and no authenticated private LMS refresh was performed, so live completion-tab compatibility, real attachment download, and Samsung rendering remain pending.

The app JVM suite (125/125), data-pipeline suite (32 pass plus one gated skip), Android-test compilation, lint, debug assembly, and R8 optimized assembly pass. API 36 emulator instrumentation executed 83 cases: 80 passed and three Samsung-physical/live-network cases were intentionally skipped. The optimized APK is `app/build/outputs/apk/optimized/app-optimized.apk`, 5,138,489 bytes, SHA-256 `E7EC60448EEB271C768BAC4AFA0279095EB3995B5F2388437F60410832F42C60`. The Galaxy was no longer present in `adb devices` at the installation gate, so this build is not claimed as installed or live-authenticated on the phone; real private item counts, detail rendering, and an attachment save remain pending after reconnection.

### Dormitory meal transient-failure diagnosis — automated and Galaxy smoke verified — 2026-09-01

Evidence: submission `8d7d15b1-323d-49da-b10c-24b4af5bc1b9` reached the `dorm-submissions` branch and triggered Actions run `33488270773`; the run checked out the submitted image but Gemini returned HTTP 503 during validation. The pipeline published a generic `ERROR` status and no `dorm_meal` dataset, confirming that Cloudflare upload succeeded and the missing retry was the failure boundary.

A red-green local HTTP test now returns 503 once and a valid schema response next. It failed before the fix and passes after bounded retry support. The complete data-pipeline suite and all three anonymous Worker contract tests pass. Optimized v1.5 (6), SHA-256 `565806F95ECE294072AE7F5D5F101BBAE7659A93924A6E6B6B00740D5FDAA94A`, replace-installed on physical `SM-S918N` serial `R3CW203NFSL`; `MainActivity` cold-launched in 526 ms, PID 9460 remained resumed, and no immediate FATAL/ANR was captured. This verifies installation/smoke only, not Samsung Now Bar rendering.

After commit `1517e18` reached `main`, the same original Actions run `33488270773` was rerun as attempt 2 without another upload. It completed validation, OCR, data commit, and Pages deployment; the public submission state became `PUBLISHED`, manifest dataset `dorm_meal` revision 1 became `READY`, and its content-addressed payload covers 2026-08-31 through 2026-09-06 with five days. On the Galaxy, one Meal-tab refresh removed all five `등록된 식단 없음` rows and the `사진 올리기` action and rendered ten meal-section headings; PID 9460 stayed alive with no related FATAL/ANR. GitHub validation run `33496960138` also passed.

### Multiple simultaneous Live Updates — physical Galaxy and user gesture verified — 2026-09-01

Evidence: on exact physical serial `R3CW203NFSL` (SM-S918N/API 36), two independently promotable DIMA Now ongoing notifications with IDs `6300` and `6301` were active simultaneously. Samsung assigned `FLAG_PROMOTED_ONGOING` to both. The notification center visibly rendered both as separate cards under `실시간 정보`; the lock-screen Now Bar visibly rendered one card at a time, and the user directly confirmed that an upward gesture advanced to the next DIMA Now bar. Status-chip rotation was not separately accepted. The relevant all-app live-information setting was enabled only for the test and restored to its prior unset state; both notifications and the test package were removed, the temporary test source was reverted, and optimized v1.5 (6) was restored with `adb install -r`. Full evidence and limitations are in `docs/now-bar-multiple-live-updates-2026-09-01.md`.

### Native LMS detail and hidden automatic-login engine — automated and Galaxy-focused verification — 2026-09-01

The previously installed build was directly observed opening an LMS item into a WebView that rendered the authenticated LMS shell instead of article content. The replacement uses a Compose-only detail body and native attachment-save actions, rejects the landing shell as invalid detail, follows the official course-session redirect while forwarding its session cookie, and keeps the official portal WebView invisible behind a native progress surface during automatic login. JVM tests and four focused API 36 tests on physical `SM-S918N` serial `R3CW203NFSL` pass. Optimized v1.5 (6), SHA-256 `5CA13B9194D44FAD8AB2D53A90C8E9A05FC6BA7726F7B03C1FDF6698BC6E244C`, was replace-installed; MainActivity cold-launched in 485 ms, remained resumed, and produced no immediate FATAL/ANR. Final authenticated visual acceptance is pending a fresh user login because a prior Gradle-connected test unexpectedly reset the app's private data.

### LMS single-session recovery — physical Galaxy verified — 2026-09-01

Evidence: a 5 fps capture reproduced the real `프로툴 사전진단` card open, sub-0.2-second native login progress, and `로그인이 필요합니다` result. The direct HTTP detail transport received a login shell while the app-owned WebView session could still render the official LMS page. The source now gives that trusted hidden session engine one form-execution attempt before declaring the whole session expired.

On exact physical serial `R3CW203NFSL`, the same card then opened as a native Compose detail with the assignment body visible, zero visible WebViews in the hierarchy, PID 13928 alive, and no captured FATAL/ANR. Focused instrumentation passed 24/24, including standard card click semantics, password EditorInfo, direct-HTTP login-shell fallback, native attachment action, Room/cache behavior, and official form transport. Logging into another LMS browser still invalidates the prior app session by official LMS policy.

### LMS notice/material structure and CONTENT fallback — verified source and automated tests — 2026-09-01

Authenticated inspection of the official `전체 학습` page and its detail routes confirmed 14 current visible rows across notice, assignment, content, and material types. Notice/material details use `table.table_view_basic`, with a native body row and `fncFileDown` authenticated attachments; the parser now handles those exact shapes. Multiple content sessions can share the same official `<course>_V` identifier, so internal list identities include the row title and remain distinct.

The first official `학습시작` action was observed to open the LMS course player rather than an article; playback was paused immediately and no completion or submission was performed. CONTENT now uses an explicit same-session in-app official course page, while notices, materials, and assignments remain native Compose details. Focused JVM and Galaxy instrumentation passed, and the full JVM, data-pipeline, Android-test compilation, lint, and optimized build are green. Optimized v1.5 (6), SHA-256 `AB827298CB5B40DB6218DB12499FACF85684A13E7D20BECB35E619A6870C49BC`, was replace-installed on `R3CW203NFSL`; `MainActivity` remained top-resumed as PID 31212 with no immediate DIMA Now FATAL or ANR. Final authenticated item-opening acceptance remains pending because the simultaneously active side-browser LMS session triggers the official single-session error.

### LMS native detail, cache-first attachment validation, and bounded session takeover — automated and Galaxy verified; video playback pending — 2026-09-02

Current source keeps `전체 학습` as the sole list input and routes NOTICE, MATERIAL, and ASSIGNMENT to structured native detail. CONTENT rows remain visible with the LMS-provided complete/incomplete state when the corresponding status pages are available; the app does not infer completion. Exact CONTENT confirmation/action selection and unsupported official fallback are covered by `LmsLearningLaunchPolicyTest`. `LmsHtmlParserTest` passes 31/31, including the real assignment-list direct-text shape, enclosing-form precedence, scoped request fields, final-URL Referer, metadata, and sanitizer-before-attachment-removal behavior. `LmsCacheMigrationTest` covers the additive LMS v4-to-v5 metadata/request migration.

The typed download path is implemented in `LmsSource`, `LmsDownloadModels`, and `LmsDocumentWriter`; focused transport, Room, and writer tests cover zero-length, declared-length mismatch, login/error HTML, non-LMS redirect, cache cleanup, null output, and partial destination copy. On physical Galaxy `SM-S918N` (`R3CW203NFSL`), notice, material, and assignment details rendered natively with zero visible article WebViews. A saved PDF was 264,542 bytes and a saved assignment MP4 attachment was 10,481,518 bytes; both nonzero results were observed only after the cache-first validation and Android document handoff.

`LmsLoginFlowReducerTest` and `LmsSessionTakeoverWebViewTest` cover exact 3045 classification, the small verified script protocol, one takeover submission, repeated-conflict failure, wrong-host rejection, and interactive-authentication refusal. A real browser/app session conflict on the same Galaxy reached the exact portal 3045 page and the app's bounded one-time takeover restored its LMS list. The official CONTENT shell and exact title were reached in the app, but the central player area remained blank during observation. Video existence and server-provided completion state are therefore the accepted primary behavior; in-app playback remains a secondary, unaccepted path and must not be reported as working until media is visibly rendered.

### LMS final session/cache security regression — automated verified; live catalog pending unlock — 2026-09-02

Independent review found that an official portal credential redirect could previously be parsed as an empty dashboard and replace last-good items. A red-green parser and Room regression now classify the exact portal credential page as `SessionExpired`, preserve cached rows, and move the session to EXPIRED. Additional red-green tests reject list-only notice/material HTML, octet-stream HTML error bodies, unverified 3045 controls, CAPTCHA/OTP/additional login fields, and direct external HTTP(S) WebView requests. Android WebView does not expose every subresource 30x destination to `shouldInterceptRequest`; SECURITY documents that platform limitation instead of claiming a complete redirect-hop allowlist.

Final automation passes 179 app JVM tests, 34 pipeline tests with one intended gated skip, Android-test compilation, lint, debug and optimized assembly, and 140 API 36 instrumentation cases with three intended Samsung/live-network skips. Optimized v1.5 (6), 5,351,481 bytes, SHA-256 `09A6F2D86934950A9DF15D9F4FB2F3EB61671DEC1D250C9856AD638A3B6E116D`, installed successfully on exact physical `SM-S918N` serial `R3CW203NFSL`; cold launch completed in 362 ms, PID 17965 remained resumed, and the minimal app-error log slice contained no immediate FATAL/ANR. The package was absent immediately before installation, so this is a fresh-install smoke rather than a data-retention claim. The phone was locked at the live acceptance gate, so a post-install authenticated refresh and visible `수강 완료`/`미수강` evidence are still pending.

### Portrait-only Android 16 tablet install — automated and physical SM-X710 verified; visible UI pending unlock — 2026-09-02

`MainActivity` declares `screenOrientation="portrait"` and the activity-scoped Android 16 `PROPERTY_COMPAT_ALLOW_RESTRICTED_RESIZABILITY` opt-out. Red-green API 36 tests first observed the previous unspecified orientation and missing property, then passed after each minimal manifest change. A separate rotation test froze the display at 90 degrees and verified that the running activity configuration remained portrait; it passed on both the API 36 emulator and exact physical Samsung tablet `SM-X710` serial `R54W703V2TZ`. The tablet's original `wm user-rotation` state was `lock 0` and was restored to the same value after testing.

Final verification passes 179 app JVM tests, 34 pipeline tests with one gated skip, Android-test compilation, lint, debug and optimized assembly, and 143 API 36 instrumentation cases with three intentional Samsung/live-network skips and no failures. Optimized v1.5 (6), 5,351,561 bytes, SHA-256 `E68A3898E616393EFD51272532D0F16A382FCDF20CBFA80CF2F68F0CF75332C8`, installed successfully on the tablet; the package was absent beforehand, so this is a fresh install. Cold launch completed in 297 ms, PID 9028 remained alive, and the app-PID diagnostic slice had no immediate FATAL/ANR. The tablet remained locked, so the five-tab visual smoke is still pending and is not claimed.

After the tablet was unlocked, the same optimized install was visually and semantically exercised through all five tabs. Home rendered the next-class, outside-campus shuttle, current student-meal, and school-notice cards; Timetable rendered the seeded Korean weekday schedule; Shuttle rendered its weekday/origin board and next departures; Meal rendered the validated current week; Classes rendered the fresh-install native LMS login surface without entering credentials. Each bottom-navigation item became the selected destination while `MainActivity` stayed focused.

The physical back sequence also passed: Settings returned to the Meal tab, the next back returned directly to Home rather than replaying older tab actions, and Home back returned to the Samsung launcher while PID 9028 remained alive. A fresh launch was visually inspected at 1600 x 2560. Requesting display rotation 90 degrees left DIMA Now in portrait and the tablet was restored to `wm user-rotation lock 0`. Installed version remained v1.5 (6), and the accumulated app-error log contained no DIMA Now FATAL/ANR. LMS authentication, attachment download, widgets, geofence walking, and Samsung Now Bar/AOD were not exercised on this tablet.

### Shuttle physical topology and anonymous missed-arrival advisory — automated, Cloudflare, and Galaxy verified — 2026-09-02

The app and Worker now share the same physical-run projection: daytime A is ONE_ROOM↔MAIN, daytime B is MAIN↔YEIN, and evening rows collapse to YEIN → MAIN stadium → ONE_ROOM → MAIN stadium → YEIN. The official 605-row cache is retained; four handwritten weekday ONE_ROOM departures at 14:30, 15:30, 16:30, and 17:30 are a separate 20-row idempotent projection with unknown arrival times left null. The maintained CSV contains 625 effective rows.

GuidanceEngine tests cover physical-loop collapse, route-local upstream propagation, and exact report-window boundaries. The app uses a fresh GPS sample before a real report, disables submission in TEST mode, shows current-stop controls and upstream advisory counts only on Home/Shuttle, and leaves widget/Live Update payloads unchanged. The Worker validates the current Pages revision and stop-call identity, stores only a server-secret install-token HMAC in D1, limits one installation per stop call, supports deletion, rate-limits requests, and prunes after seven days.

Verification passed 186 app JVM tests, 35 pipeline tests with one intended gate skip, six Worker tests, Android-test compilation, lint, debug/optimized assembly, and 147 physical API 36 instrumentation executions with three intentional Samsung/live-network skips. An initial physical run exposed only a Proton Pass save-dialog focus interruption; the exact autofill service was restored, the focused seven tests passed, and the clean full rerun had zero failures. Optimized v1.5 (6), 5,401,105 bytes, SHA-256 `824887C4E65B12A0D6A41B649C2D17454C74500CF39F0B9A1065A44B2AECA68B`, was installed on SM-S918N serial `100.112.73.34:5555`; PID 23971 remained top-resumed with no immediate FATAL/ANR. The package was absent after instrumentation cleanup before final install, so this is not a retained-data claim. The fresh install remains at the HomeBase chooser and no choice was guessed.
